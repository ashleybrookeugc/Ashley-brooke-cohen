# Work Session Handoff

- **Repo / branch:** `ashleybrookeugc/Ashley-brooke-cohen` / `portfolio-v2`. The remote branch is authoritative; the local checkout is stale and contains an unrelated ahead commit and untracked user files. Do not push that checkout or touch `main`.
- **Current objective:** Phase 2C calibration of the frozen 12 Ashley URLs for her online-business-card video rubric. This handoff request pauses that work; no further product changes were made.
- **Working:** Earlier sessions reported the internal analyzer working on YouTube, Instagram, and TikTok (not retested here). This session downloaded all 12 sampled complete media files, decoded beginning/end frames and audio, saved original caption/metadata, draft timestamped ASR, OCR observations, four actual timeline JPEGs per clip, full-length compressed audio, media hashes, and source URLs in `docs/PORTFOLIO_VIDEO_CALIBRATION_EVIDENCE.json`. All 12 source retrieval records are `2/2`; a remote fetch parsed all 12 successfully.
- **Incomplete / real blocker:** ASR and OCR are machine drafts, **not** human-verified (clip 09 appears to have a spurious “Thanks for watching!” ASR segment). No independent board ratings, Chair reconciliations, agreement denominators, metric passes, anchor revisions, or `docs/PORTFOLIO_VIDEO_CALIBRATION_RESULTS.md` exist. The frozen policy/protocol say calibration uses unrelated creators and forbids Ashley clips; the latest explicit user instruction instead requires the frozen Ashley sample. Document this deviation in results, without silently rewriting the policy. One rating per distinct perspective cannot establish the required **within-perspective** agreement; obtain independent comparable ratings before computing thresholds. Do not invent evaluators or agreement.
- **Decisions / constraints:** Authorship is not scored; Ashley is default creator/editor for her own `@ashleybrookecohen` posts absent a specific collaboration/external-production signal. No old Feature/Keep/Skip labels, generated titles, popularity metrics, performance inferences, universal composite, averages of disagreements, other library scoring, analyzer changes, or public portfolio edits. Keep N/O distinct from zero; use only `N/O—FORMAT` or `N/O—CORPUS`. Source retrieval `2/2` does not certify ASR/OCR accuracy.
- **Files changed in this session:** `docs/PORTFOLIO_VIDEO_CALIBRATION_EVIDENCE.json`; `docs/TROUBLESHOOTING_LOG.md` (reusable large-file truncation fix); this handoff. No application files changed.
- **Relevant commits:** `8e2ebc4e9ec52de3fdc61a8a3793b87914a316ac` complete evidence JSON; `d09c37794918fbaeb07a180009e66c3e1c314114` troubleshooting recovery. The handoff commit follows.
- **Checks:** 12/12 source files downloaded (clip 12 needed a second attempt; early SSL certificate errors were resolved on retry); ffprobe found video/audio and beginning/end frames decoded; 12/12 compact remote JSON items parse at `2/2`; contact sheet visually sampled four points per item. No board agreement calculations were run. Local source MP4s are transient under `/tmp`, so use the persisted audio/frames and original URLs when auditing.
- **Next, in order:** (1) Verify the remote `portfolio-v2` state and read the governing documents below. (2) Verify full transcript/on-screen text against source audio/video; correct or flag ASR/OCR errors, particularly clip 09. (3) Establish genuinely independent, comparable same-perspective evaluator records and explicitly record the Ashley-sample override. (4) Run the nine-role board on eligible clips with timestamp citations, audit and reconcile disagreements, preserving minority views. (5) Compute raw agreement/N/O denominators and Version 1 pass/fail, document necessary metric-specific revisions/retests, and save `docs/PORTFOLIO_VIDEO_CALIBRATION_RESULTS.md`; do not score other videos.
- **Read before editing:** `AGENTS.md`, `docs/TROUBLESHOOTING_LOG.md`, `docs/PORTFOLIO_VIDEO_RESEARCH_SPEC.md`, `docs/PORTFOLIO_VIDEO_RUBRIC_RESEARCH.md`, `docs/PORTFOLIO_VIDEO_RUBRIC_POLICY.md`, `docs/PORTFOLIO_VIDEO_CALIBRATION_PROTOCOL.md`, `docs/PORTFOLIO_VIDEO_CALIBRATION_SAMPLE.md`, and the evidence JSON.
