# Work Session Handoff

- **Repo / branch:** `ashleybrookeugc/Ashley-brooke-cohen` / `portfolio-v2`. Remote branch authoritative; do not push stale local checkout or touch `main`.
- **Objective / scope:** Phase 2C Step 2 of the frozen 12 Ashley clips. Only evidence JSON and this handoff changed. No board, ratings, agreement, rubric changes, other videos, analyzer, or portfolio work.
- **Full source access:** Re-retrieved and decoded all 12 complete source MP4s. Eleven TikTok files match saved SHA-256 and byte counts; clip 05 Instagram is a fresh 94.251s remux with saved/fresh audio correlation 0.944. Extracted a full-resolution frame every second through each end (262 frames total), inspected timeline sheets and targeted full-resolution frames, and ran OCR across those frames. The new evidence JSON logs 42 timestamped directly visible text observations; sub-second transient and tiny fine print are not exhaustively verified.
- **Audio:** All 12 persisted full-length Opus streams decoded; independently cross-checked original ASR with Whisper small and medium. This environment cannot **directly audition** audio via the available model interface. Accordingly `transcript_verified_against_audio` remains false for every item, and `on_screen_text_verified_against_full_video` remains false for frame-exhaustive certification. Do not treat either machine output or retrieval `2/2` as human verification.
- **Corrections:** Clip 04 question is confirmed by the full adjacent subtitle sequence and both ASR passes; corrected the earlier cropped-frame reading. Clip 05 visible graphic reads **345,000**, names **Publix GreenWise** and **Frutas y Hortalizas**, and shows lot codes **06453, 60401, 12053, 0653G, 12153**; corrected garbled ASR and marked source graphic vs subtitle date separately. Clip 08 independent ASR passes agree on wording that differs from draft, but direct audition still required. Clip 09's `Thanks for watching!` draft was removed as an ASR artifact: no speech intervals in complete 10s audio by VAD, small ASR returned music, medium hallucinated a different generic call to action; no direct listening, so a faint mixed vocal cannot be categorically ruled out. Clip 12 corrected the stable `don't have this at home` wording, retaining an unresolved `we/you` pronoun.
- **Other uncertainties:** Clip 01 final subtitle says `BRO RECOGNISE BRO` while both ASR passes say `recognize growth`; clip 02 subtitle says Upper East while both ASR passes hear upperish west; clip 04 sunflower pronoun differs across caption/ASR; clip 12 pronoun and 3–4s caption/speech alignment are unresolved. On-screen legal/product fine print too small to quote. Source statements about recalls/lawsuits were transcribed as content, not externally fact-checked.
- **Files / commits:** `docs/PORTFOLIO_VIDEO_CALIBRATION_EVIDENCE.json` committed as `48580204a3741b158528476e5c7da5f44e0a38ac`; this handoff commit follows. Remote evidence re-fetched and parsed: 12 records, 42 verified visible-text entries, clip 09 transcript empty.
- **Next:** If exact audible words and exhaustive text are required, use an audio-capable reviewer to listen to each complete MP4 and inspect all frames/transient text, resolve flagged conflicts, then update only supported evidence and verification flags. Do not proceed to later calibration steps without a new explicit user request.
- **Read before editing:** This handoff and `docs/PORTFOLIO_VIDEO_CALIBRATION_EVIDENCE.json`. Consult other governing files only if later scope genuinely requires them.
