# Work Session Handoff

- **Repo / branch:** `ashleybrookeugc/Ashley-brooke-cohen` / `portfolio-v2`. Do not touch or merge to `main`.
- **Foundation:** Continue from the commit containing this handoff. It extends audited foundation `e04e2fa901544bf6b02654cc282e165817836ded` without replacing its seven original tests or real-media primitives.
- **Verified intake / identity:** Direct local files and retrievable direct-video URLs use the same pipeline. Source bytes are SHA-256 hashed into stable `mediaasset-sha256-<full hash>` identity; a regression test proves identical local/URL bytes produce the same asset and content-version IDs. URL failures still distinguish automated blocking, removed/unavailable, temporary failure, and retrievable non-media/unsupported source. Social-page scraping is not claimed.
- **Verified decode / package:** ffprobe inspection and full ffmpeg end-to-end decode remain mandatory. The package contains fixed-cadence frames, an audio derivative when present, `manifest.json`, and `evidence.json`. Local read-back validates required components, synchronized-timeline structure/provenance, and the evidence SHA-256.
- **Verified ASR:** A pinned ignored runtime installs PocketSphinx 5.0.4. The smallest 6.86-second dialogue fixture recovers two timestamped spans exactly: “i do not think this is fair because you know the answer” and “why not because i already checked it.” Word timestamps/confidence, extractor/model/version, and the no-direct-audition limitation are recorded. Unavailable and failing extractors remain explicit and fail deletion eligibility.
- **Speaker-turn limit:** Silence-bounded ASR utterances become separate timestamped turns with `speaker_unknown`. This is a bounded turn primitive, not voice-cluster diarization or speaker identity. Never rename a speaker Ashley without independent evidence.
- **OCR / visual evidence:** Tesseract TSV includes runtime version, confidence, bounds, and frame provenance. Layout plus temporal ASR overlap classifies burned-in-caption candidates separately from other screen/UI text. The test preserves transient `FLASH NOTICE`, unrelated `EXPORT`, and unresolved disagreement between spoken negation and the visible caption. Sampled 64x64 motion/state plus OCR keyword evidence produces editing-workflow, unrelated-browsing/watching, or indeterminate activity candidates. It remains low-confidence, sampled/not frame-exhaustive, and does not robustly recover subjects/objects.
- **Synchronized representation:** Each lane stays separate. The canonical timeline retains timestamps, lane, observation ID, verification modality, source references, uncertainty, and explicit modality-conflict events. Corrupt timeline references and a missing semantic lane are rejected on read-back.
- **Persistence boundary:** `persistence.local` is verified but explicitly scoped to `current_machine_or_workspace`. `persistence.remote` is `not_configured/not_run/local_only`. A provider-neutral remote-store contract requires stable locator plus digest-verified read-back; its corruption failure is tested with a double, but no concrete remote provider or future-session retrieval is claimed.
- **Storage decision required:** The canonical UGC lifecycle already approves iCloud Drive for UGC media and treats the phone-to-analysis Share/upload bridge as a prototype candidate requiring explicit promotion. Reusing that bridge is the smallest aligned option, but Ashley must approve its promotion and a real iPhone/iCloud round-trip must pass. Alternatives require separate approval: Google Drive integration (15 GB account storage shared with Gmail/Photos) or Cloudflare R2 (10 GB-month Standard free tier, but R2 subscription/checkout and paid overage path). Do not create a new repo or paid path by assumption.
- **Safe-delete state:** Local verification alone cannot pass. The gate also requires every evidence lane to be complete (bounded/heuristic states do not qualify), remote write, remote digest-verified read-back, and verified future authorized retrieval. No source is automatically deleted. Current packages are `safe_to_delete_original: false`.
- **Tests run:** Syntax checks passed for core, understanding, setup, and CLI. `npm run test:video-analyzer` passes 13/13. Original positives/negatives remain passing; additions prove exact bounded ASR wording, ASR unavailable/failure, speaker turns, caption/UI/transient classification, modality disagreement, sampled workflow-vs-browsing states, corrupted timeline, missing semantic evidence, stable cross-intake MediaAsset identity, corrupted remote read-back, and local-only non-deletability. `npx wrangler deploy --dry-run` also succeeds with the existing D1 binding.
- **Inspected artifact:** A real generated 6.86-second dialogue MP4 package was read directly. It had two exact ASR spans, two unknown-speaker turns, caption/UI/transient OCR observations, unresolved spoken-caption conflicts, 27 sampled frames, 113 provenance-bearing timeline events, complete decode through 6.848 seconds, verified local digest/read-back, remote `not_configured`, and `safe_to_delete_original: false`.
- **Legacy route:** `/api/admin/video-analysis` remains correctly labeled `METADATA_ONLY`; it is not this pipeline.
- **Hour-long recording:** Not available in this workspace and not processed. Do not claim otherwise or ask Ashley to delete it.
- **Next actions:** (1) Ashley chooses whether to promote the existing iCloud Drive phone-to-analysis bridge or approve another provider; (2) implement the chosen authenticated provider against the remote-store contract and verify retrieval/digest in a genuinely separate authorized session; (3) replace/broaden bounded speaker-turn and semantic subject/object/action extraction enough to satisfy the evidence policy; (4) run a real phone-upload acceptance fixture; (5) only then run and inspect the hour-long recording as the priority end-to-end regression.
- **Read before editing:** `AGENTS.md`, `docs/TROUBLESHOOTING_LOG.md`, this file, `docs/PORTFOLIO_VIDEO_RESEARCH_SPEC.md`, and the canonical research-vault `AGENTS.md`, `ACTIVE_WORK.md`, `PROJECT_INDEX.md`, `shared-capabilities/video-understanding/EVIDENCE_STANDARD.md`, and `shared-capabilities/ai-workflows/SESSION_AND_FAILURE_PROTOCOL.md`. Also inspect `ashleybrookeugc/ugc-creator-app/docs/MEDIA_ASSET_LIFECYCLE.md` before changing storage.
- **Exact remaining blockers:** No approved/verified remote provider or future-session read-back; semantic subject/object/action evidence remains bounded and incomplete; robust multi-speaker diarization is not implemented; the actual hour-long source is unavailable here.
