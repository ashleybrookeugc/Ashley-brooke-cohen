{
  "name": "ashley-brooke-cohen",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "node --test",
    "check:worker": "wrangler deploy --dry-run --outdir /tmp/control-worker-build"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
